#include "hashmap.h"

HashMap* InsertSubsToHashTable(char *fullFileNamePath, int allSubsLinesLength);
char* GetSubtLineFromHash(char curtime[50], HashMap *hm);
char* substring(const char* str, size_t begin, size_t len);
